import React from 'react'

const About = () => {
  // const a = useContext(noteContext);
  return (
    <div>
      These is Note app . It is made using React JS , node , express and MongoDB Compass as a database.
      The user can store the notes is here after making an account . The user can only view and edit his own notes.
      For using the notes of others he would have to enter their credentials.
    </div>
  )
}

export default About
